    posts a new user
    ---
    tags:
      - Auth
    parameters:
      - name: user_and_password
        in: body
        required: true
        requires:
          - email:
          - password:
        properties:
          email:
            type: string
          password:
            type: string
          name:
            type: string
          age:
            type: integer
          role:
            type: integer
            # description: 0 for admin, 1 for student, 2 for teacher

    responses:
      201:
        description: successfully created user
